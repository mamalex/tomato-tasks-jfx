package ru.mamalex.pomodorotasks;

import javafx.scene.control.Label;
import javafx.scene.layout.BorderPane;

public class TaskLine extends BorderPane {
    /*
    private Task task;
    private boolean isRunned = false;
    private long spendingTime = 0;

    TaskLine(Task task){
        this.task = task;
        setLeft(new Label(task.getTitle()));
        setCenter(new Label(String.valueOf(spendingTime)));
    }
    *
     */
}